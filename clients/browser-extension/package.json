{
  "name": "unimem-browser-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chromium MV3 connector that saves a page selection or a whole-page HTML snapshot to the local UniMem capture API.",
  "scripts": {
    "test": "node --test"
  }
}
